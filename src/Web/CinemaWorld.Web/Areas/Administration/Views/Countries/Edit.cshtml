@model CinemaWorld.Models.ViewModels.Countries.CountryEditViewModel
@{
    this.ViewData["Title"] = "Edit Country";
    this.ViewData["ActivePage"] = CountryNavPages.Countries;
}

<div class="row">
    <div class="col-md-6">
        <form asp-area="Administration" asp-controller="Countries" asp-action="Edit" method="post">
            <h4>Edit <b>@Model.Name</b> country with id: @Model.Id</h4>
            <hr />
            <div class="form-group">
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-success">Edit country</button>
            <button asp-area="Administration" asp-controller="Countries" asp-action="GetAll" class="btn btn-primary">Back to all countries</button>
        </form>
    </div>
</div>