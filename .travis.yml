dist: xenial
addons:
  snaps:
  - name: dotnet-sdk
    confinement: classic
    channel: latest/beta
sudo: required
language: csharp
solution: /CinemaWorld/src/CinemaWorld.sln
install: nuget restore /CinemaWorld/src/CinemaWorld.sln
mono: none
script:
  - sudo snap alias dotnet-sdk.dotnet dotnet
  - dotnet --version
  - dotnet build